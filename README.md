
  # Student Sign-Up Flow

  This is a code bundle for Student Sign-Up Flow. The original project is available at https://www.figma.com/design/mTHclArqfhX6EulMTeiGxd/Student-Sign-Up-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  